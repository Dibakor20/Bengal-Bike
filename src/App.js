import React from "react";
import {
  Routes,
  Route,
} from "react-router-dom";
import './App.scss';
import AppContext from "./context/AppContext";

function App() {
  return (
    <AppContext>
        {/* <Routes>
        <Route path="/" element={ }/>
        </Routes> */}
     </AppContext>
  );
}

export default App;
